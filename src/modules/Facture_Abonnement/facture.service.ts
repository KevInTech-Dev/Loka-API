import { FactureAbonnement } from "@/database/models/FactureAbonnment";
import { CreateFactureAbonnementInput } from "./facture.schema";

export class FactureAbonnementService {

    //creer une pour un abonnement
    // createFactureAbonnement = async (datas: CreateFactureAbonnementInput): Promise<FactureAbonnement> => {
    //     // verifier si une facture existe
    //     existingInvoice = 
    // }

}